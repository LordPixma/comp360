{
  "name": "@c360/api-worker",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
  "build": "esbuild src/index.ts --bundle --platform=browser --format=esm --outfile=dist/index.js",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "hono": "^3.10.0",
  "@c360/core": "file:../../packages/core",
  "@c360/shared": "file:../../packages/shared",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "esbuild": "^0.19.0"
  }
}